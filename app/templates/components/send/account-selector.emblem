= power-select [
    options=accounts
    selected=selected
    searchEnabled=false
    placeholder=(t 'paysend.form.account.ph')
    onchange=(action "changedAccount")
] as |c|
  span.label.colored-bg class=c.color style='min-width: 24px; margin-right: 5px;' = c.identifier
  b = c.name
